<%= render 'shared/header2' %>
<br>
<div class="container col-11">
        <div class="d-flex">
            <div class="container">
               <h2 class="display-5"> <%= t('.title')%></h2>
            </div>
        </div>
        <div class="container">
            <%= link_to t('.create_standar'), new_standar_path, class: 'btn btn-xs btn-primary' %>
        </div>
        <hr>
    <table class="table table-hover">
        <thead> 
            <tr>    
                <th scope="col"><%= t('.name') %></th>
                <th scope="col"><%= t('.description') %></th>
                <th scope="col"><%= t('.value') %></th>
                <th scope="col"><%= t('.cycle') %></th>
                <th scope="col">Norma</th>
                <th scope="col"><%= t('common.edit') %></th>
            </tr>
        </thead>
        <tbody>
            <%= render partial: 'standar', collection: @standars  %>  
        </tbody>      
    </table>
</div>