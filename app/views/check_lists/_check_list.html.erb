<tr>
    <td><%= check_list.id %></td>
    <td><%= check_list.name %></td>
    <td><%= CheckList.label_state(check_list.state) %></td>

    <td><%= link_to t('common.edit'), edit_check_list_path(check_list), class: 'btn btn-sm btn-primary'  %></td>
    <td><%= link_to 'Item', new_check_list_item_path(check_list.id), class: 'btn btn-sm btn-primary'  %></td>

</tr>