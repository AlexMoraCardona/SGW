<!DOCTYPE html>
<head>
    <meta charset='utf-8' />
</head>
<%= javascript_include_tag "https://www.google.com/jsapi" %>
<%= wicked_pdf_javascript_include_tag "chartkick" %>
<%= wicked_pdf_javascript_include_tag "application", "chartkick" %>


<div class="container col-12 card p-5"> 
    <div>
        <br>
    </div>

    <div><br></div>    
    <div class="row text-center">
        <strong>ESTADÍSTICAS ACCIDENTES DE TRABAJO AÑO <%= @evidence.year_initial if @evidence.present? %></strong>
    </div> 
    <div><br></div>   
    <div class="row g-3">
        <div class="col-sm-7">
            <%= column_chart  @at, title: "Accidentes de trabajo", download: {filename: "AccidentesTrabajo"}, download: {background: "#FFFFFF"}, xtitle: "Mes", ytitle: "Cantidad Accidentes de Trabajo", suffix: " Accidentes" if @at.present? %>
        </div>
        <div class="col-sm-1">
        </div>
        <div class="col-sm-4">
            <br>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">Mes</th>
                        <th scope="col">Accidentes de Trabajo</th>
                    </tr>
                </thead>
                <tbody>
                    <%  total = 0 %>
                    <%  if @at.present? %>
                        <%  i = 0 %>
                        <%  while i < 12 %>
                            <tr>
                                <td><%= @at[i][0] %></td>
                                <td style="text-align:center"><%= @at[i][1] %></td>
                                <% total = total +  @at[i][1].to_i %>
                            </tr>   
                            <%  i += 1 %> 
                        <% end %>
                        <tr>
                            <td>Total</td>
                            <td style="text-align:center"><%= total %></td>
                        </tr>   

                    <% end %>
                </tbody>
            </table>
        <hr>
    </div>

    <div><br></div>   
    <div class="row g-3">
        <div class="col-sm-7">
            <%= column_chart  @ca, title: "Parte del Cuerpo Afectada", download: {filename: "Parte del Cuerpo Afectada"}, download: {background: "#FFFFFF"}, xtitle: "Detalle", ytitle: "Total", suffix: " Total" if @ca.present? %>
        </div>
        <div class="col-sm-1">
        </div>
        <div class="col-sm-4">
            <br>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">Parte del Cuerpo Afectada</th>
                        <th scope="col">Cantidad</th>
                    </tr>
                </thead>
                <tbody>
                    <%  total = 0 %>
                    <%  if @ca.present? %>
                        <%  @ca.each do |ca| %>
                            <tr>
                                <td><%= ca[0] %></td>
                                <td style="text-align:center"><%= ca[1] %></td>
                                <% total = total +  ca[1].to_i %>
                            </tr>   
                        <% end %>
                        <tr>
                            <td>Total</td>
                            <td style="text-align:center"><%= total %></td>
                        </tr>   

                    <% end %>
                </tbody>
            </table>
        <hr>
    </div>

    <div><br></div>    
    <div class="row text-center">
        <strong>ESTADÍSTICAS ENFERMEDADES LABORALES AÑO <%= @evidence.year_initial if @evidence.present? %></strong>
    </div> 
    <div><br></div>   
    <div class="row g-3">
        <div class="col-sm-7">
            <%= column_chart  @el, title: "Enfermedades laborales", download: {filename: "EnfermedadesLaborales"}, download: {background: "#FFFFFF"}, xtitle: "Mes", ytitle: "Cantidad Enfermedades Laborales", suffix: " Enfermedades" if @el.present? %>
        </div>
        <div class="col-sm-1">
        </div>
        <div class="col-sm-4">
            <br>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">Mes</th>
                        <th scope="col">Enfermedades Laborales</th>
                    </tr>
                </thead>
                <tbody>
                    <%  total = 0 %>
                    <%  if @el.present? %>
                        <%  i = 0 %>
                        <%  while i < 12 %>
                            <tr>
                                <td><%= @el[i][0] %></td>
                                <td style="text-align:center"><%= @el[i][1] %></td>
                                <% total = total +  @el[i][1].to_i %>
                            </tr>   
                            <%  i += 1 %> 
                        <% end %>
                        <tr>
                            <td>Total</td>
                            <td style="text-align:center"><%= total %></td>
                        </tr>   

                    <% end %>
                </tbody>
            </table>
        <hr>
    </div>

    <div><br></div>   
    <div class="row g-3">
        <div class="col-sm-7">
            <%= column_chart  @cael, title: "Parte del Cuerpo Afectada", download: {filename: "Parte del Cuerpo Afectada"}, download: {background: "#FFFFFF"}, xtitle: "Detalle", ytitle: "Total", suffix: " Total" if @cael.present? %>
        </div>
        <div class="col-sm-1">
        </div>
        <div class="col-sm-4">
            <br>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">Parte del Cuerpo Afectada</th>
                        <th scope="col">Cantidad</th>
                    </tr>
                </thead>
                <tbody>
                    <%  total = 0 %>
                    <%  if @cael.present? %>
                        <%  @cael.each do |cael| %>
                            <tr>
                                <td><%= cael[0] %></td>
                                <td style="text-align:center"><%= cael[1] %></td>
                                <% total = total +  cael[1].to_i %>
                            </tr>   
                        <% end %>
                        <tr>
                            <td>Total</td>
                            <td style="text-align:center"><%= total %></td>
                        </tr>   

                    <% end %>
                </tbody>
            </table>
        <hr>
    </div>

</div>        
<br>
<hr>




</div>
<br>    