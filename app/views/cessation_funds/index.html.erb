<%= render 'shared/header2' %>
<br>
<div class="container col-11">
        <div class="d-flex">
            <div class="container">
               <h2 class="display-5"> <%= t('.title')%></h2>
            </div> 
        </div>
        <div class="container">
            <%= link_to t('.create_cessation_fund'), new_cessation_fund_path, class: 'btn btn-xs btn-primary' %>
        </div>
        <hr>
    <table class="table table-hover">
        <thead> 
            <tr>    
                <th scope="col"><%= t('.id') %></th>
                <th scope="col"><%= t('.name') %></th>
                <th scope="col"><%= t('.nit') %></th>
                <th scope="col"><%= t('.code') %></th>
                <th scope="col"><%= t('common.edit') %></th>
            </tr>
        </thead>
        <tbody>
            <%= render partial: 'cessation_fund', collection: @cessation_funds  %>  
        </tbody>      
    </table>
</div>