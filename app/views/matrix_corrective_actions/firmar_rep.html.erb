<div class="container col-11 card p-5">
    <br>
    <%= form_with model: @matrix_corrective_action do |form| %>
        <% @matrix_corrective_action.errors.full_messages.each do |error| %>
            <div class="alert alert-danger" role="alert">
                <%= error%> <%= alert %>
            </div>
        <% end %>

        <table>
            <tr>
                <td>
                    <h4>Representante Legal</h4> 
                    <%= @matrix_corrective_action.name_user(@matrix_corrective_action.user_legal_representative) %>
                </td>
            </tr>
            <tr>
                <td>
                    <h4>Firma autorizada del Representante Legal</h4> 
                    <%= form.radio_button :firm_legal_representative, 0 %>
                    <label for="no">No</label>&nbsp;&nbsp; 
                    <%= form.radio_button :firm_legal_representative, 1 %>
                    <label for="si">Si</label>&nbsp;&nbsp;   
                </td>
            </tr>
        </table>
        <%= form.hidden_field :date_firm_legal_representative, :value => Time.now, class: "form-control" %>
        <br>    
        <div class="d-grid gap-2 d-md-flex">
            <%= form.button type: :submit, class: "btn btn-success" do %>
              <i class="bi bi-check-circle"></i>
            Guardar
            <% end %>

            <%= link_to :back, class: "btn btn-secondary" do %>
            <i class="bi bi-arrow-left-circle"></i>
            Retornar
            <% end %>
        </div>
    <% end %>
</div>        
<br>
