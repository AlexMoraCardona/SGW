<div class="container col-5 card p-5 text-dark">
    <br>
    <div>    
        <div>
            <p><b>Lista de peligros</b></p>
        </div>     
        <div style="text-align: justify;">
            <p>A continuación, encontrará una lista de peligros y riesgos a los que se puede exponer un trabajador en su entorno laboral. Por favor seleccione los riesgos que aplican para su puesto de trabajo.</p>
        </div>          
        <div></br></div>
        <table> 
            <% @clasification_dangers.each do |clasification_danger| %> 
                <tr>
                    <td style="border:0px solid black;"> <%= clasification_danger.name %></td> 
                </tr>
                <% @form_preventions.where("clasification_danger_id = ?", clasification_danger.id).each do |form_prevention| %> 
                    <tr>
                        <td style="border:1px solid black;width: 70%;"> <%= form_prevention.clasification_danger_detail.name %></td> 
                        <td style="text-align:center;border:1px solid black;width: 20%;"> 
                            <% if form_prevention.exposed == 0 then %>No<% end %>
                            <% if form_prevention.exposed == 1 then %>Si<% end %>
                        </td>
                        <td style="text-align:center;border:1px solid black;width: 10%;"> <%= link_to  t('common.edit'), encuestapre_path(form_prevention.id), class: 'btn btn-sm btn-outline-dark'%></td>
                    </tr>
                <% end %>  
                <tr>
                    <td style="border:0px solid black;"></br></td> 
                </tr>
            <% end %>  
        </table>
    </div>
    <div style="text-align:center;"> 
        <%= link_to '<button class="btn btn-sm btn-outline-dark" type="button">Finalizar encuesta</button>'.html_safe, form_preventions_path(entity_id: @entity.id) %>
    </div>

</div> 

<br>
