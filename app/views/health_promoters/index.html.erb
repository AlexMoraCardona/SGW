<!-- render 'shared/header2' -->
<br>
<div class="container col-11">
        <div class="d-flex">
            <div class="container">
               <h2 class="display-5"> <%= t('.title')%></h2>
            </div> 
        </div>
        <div class="container">
            <%= link_to t('.create_health_promoter'), new_health_promoter_path, class: 'btn btn-xs btn-primary' %>
        </div>
        <hr>
    <table class="table table-hover">
        <thead> 
            <tr>    
                <th scope="col"><%= t('.id') %></th>
                <th scope="col"><%= t('.name_entity') %></th>
                <th scope="col"><%= t('.code_entity') %></th>
                <th scope="col"><%= t('.nit_entity') %></th>
                <th scope="col"><%= t('.regime_entity') %></th>
                <th scope="col"><%= t('common.edit') %></th>
            </tr>
        </thead>
        <tbody>
            <%= render partial: 'health_promoter', collection: @health_promoters  %>  
        </tbody>      
    </table>
</div>

