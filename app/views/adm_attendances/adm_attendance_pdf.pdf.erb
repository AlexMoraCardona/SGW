<head>
    <meta charset='utf-8' />
</head>
<div class="container col-11 card p-5"> 
    <table class="table">
        <thead> 
            <tr>    
                <td style="width: 40%;"><strong>Nombre</strong></td>
                <td style="width: 20%;"><strong>Documento</strong></td>
                <td style="width: 20%;"><strong>Cargo</strong></td>
                <td style="width: 20%;"><strong>Área</strong></td>
            </tr>
        </thead>
        <tbody>
            <% if  @attendances.present? %>
                <%  @attendances.each do |attendance| %>
                    <tr>
                        <td style="width: 40%;"><%= attendance.user.name %></td>
                        <td style="width: 20%;"><%= attendance.user.nro_document %></td>
                        <td style="width: 20%;"><%= User.label_activity(attendance.user.activity) if attendance.user.activity.present? %></td>
                        <td style="width: 20%;"><%= User.label_area_employee(attendance.user.area_employee) if attendance.user.area_employee.present? %></td>
                    </tr>
                <% end %>
            <% end %>        
        </tbody>      
    </table>
</div>

